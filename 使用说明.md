# Codex 桌宠 Win7 v4.6

## 基本操作

- 单击宠物：挥手或跳跃。
- 双击宠物：立即在脚下显示一个单词。
- 拖动宠物：移动并保存位置。
- 设置与退出：统一放在系统托盘，宠物本身没有右键菜单。

## 脚下单词提示

单词直接显示英文、音标、中文释义和发音按钮，不再打开独立大窗口，也没有“查看答案”。只需选择“认识”或“不认识”：

- 认识：按间隔记忆法安排下一次复习。
- 不认识：显示简短记忆提示，4 秒后收起，并在 10 分钟后再次复习。

提示采用深色半透明小气泡，默认放在宠物脚下；屏幕底部空间不足时自动移到上方。提示不会自行消失，必须点击“认识 / 不认识”或按 `Esc` 关闭。

## 自动背词

- 默认启用，每 30 分钟自动提示一次。
- 每天提醒次数不限。
- 默认每天安排 10 个新词；达到新词数量后继续提醒复习词。
- 到期词优先，没有到期词时再安排新词。
- 锁屏或全屏应用期间默认暂停，不抢当前程序的输入焦点。
- 双击宠物可随时额外背一个词，不受自动提醒间隔限制。

托盘设置可调整自动提醒开关、间隔、每日新词量、词库、自动发音和全屏暂停。

## 喝水提醒

喝水提醒也显示为宠物脚下的小气泡，提供“喝过了”和“稍后”两个按钮。默认每 60 分钟提醒，可修改间隔和稍后提醒时间。锁屏期间暂停。

## 防锁屏与稳定性

防锁屏使用 Windows `GetLastInputInfo` 轮询真实操作时间，不安装全局键盘或鼠标钩子。程序按设置间隔发送极小的模拟鼠标移动；达到设定的真实无人操作时间后仍会主动锁屏。

异常日志位于 `%LOCALAPPDATA%\CodexPetWin7\crash.log`。程序只允许同时运行一个实例。

## 系统要求与文件

- 支持 Windows 7 / 10 / 11，需 .NET Framework 4.0 或更高版本。
- `Codex桌宠-Win7-v4.6.exe`：可直接运行程序。
- `CodexPetWin7.cs`：完整源代码。
- `build-win7.ps1`：Win7 编译脚本。
- `wordbooks.tsv`：离线词库。
- `assets/codex-spritesheet-win7.png`：消除粉边的 Win7 透明精灵图。
