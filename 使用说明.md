# Codex 桌宠 Windows 版 v6.0

![Codex 桌宠外观](docs/images/pet-preview.png)

## 基本操作

- 单击宠物：挥手或跳跃。
- 双击宠物：立即在脚下显示一个单词。
- 拖动宠物：移动并保存位置。
- 设置与退出：统一放在系统托盘，宠物本身没有右键菜单。

## 外观主题

系统托盘右键打开“设置”，进入“桌面宠物”页，可在以下主题间切换：

- Codex 蓝色：原有蓝色 Codex 风格宠物。
- 新增主题：真人风格人物、长黑发、浅蓝牛仔外套、白色上衣、米白长裙和白鞋。

点击“保存”后立即切换，无需重启。软件会记住当前选择；更换主题只改变宠物外观，不影响位置、大小、背词进度、喝水提醒或防锁屏设置。如果新主题资源无法加载，软件会记录日志并安全切回 Codex 蓝色。

## 脚下单词提示

![脚下单词气泡](docs/images/word-bubble.png)

单词出现时显示英文和音标，默认隐藏中文释义。点击发音按钮旁的“释”可在原位置显示或隐藏释义。只需选择“认识”或“不认识”：

- 认识：根据是否查看释义、单词难度和既往记录，自适应安排下一次复习。
- 不认识：在约 10 分钟后再次复习。

如果选择前没有手动查看释义，点击“认识”或“不认识”都会立即显示中文释义，并在 4 秒后关闭；这 4 秒内可以修改选择，最终按最后一次选择保存。如果已经点击“释”看过中文，选择后直接关闭。

提示采用深色半透明小气泡，默认放在宠物脚下；屏幕底部空间不足时自动移到上方。没有空格快捷键，提示必须点击“认识 / 不认识”或按 `Esc` 关闭。

## 自动背词

- 默认启用，每 30 分钟自动提示一次。
- 每天提醒次数不限。
- 默认每天安排 10 个新词；达到新词数量后继续提醒复习词。
- 到期词优先，没有到期词时安排新词或当前记忆最薄弱的旧词。
- 锁屏或全屏应用期间默认暂停，不抢当前程序的输入焦点。
- 双击宠物可随时额外背一个词，不受自动提醒间隔限制。

托盘设置可调整自动提醒开关、间隔、每日新词量、词库、自动发音和全屏暂停。

![单词学习设置](docs/images/settings.png)

## 喝水提醒

喝水提醒也显示为宠物脚下的小气泡，提供“喝过了”和“稍后”两个按钮。默认每 60 分钟提醒，可修改间隔和稍后提醒时间。锁屏期间暂停。

## 防锁屏与稳定性

防锁屏使用 Windows `GetLastInputInfo` 轮询真实操作时间，不安装全局键盘或鼠标钩子。程序按设置间隔发送极小的模拟鼠标移动；达到设定的真实无人操作时间后仍会主动锁屏。

异常日志位于 `%LOCALAPPDATA%\CodexPetWin7\crash.log`。程序只允许同时运行一个实例。

## 系统要求与文件

- 支持 Windows 7 / 8 / 8.1 / 10 / 11，需 .NET Framework 4.0 或更高版本。
- `Codex桌宠-v6.0.exe`：可直接运行程序。
- `CodexPetWindows.cs`：完整源代码。
- `build-windows.ps1`：Windows 编译脚本。
- `wordbooks.tsv`：离线词库。
- `assets/codex-spritesheet-windows.png`：经过透明边缘处理的 Windows 精灵图。
- `assets/new-theme-spritesheet-windows.png`：新增主题精灵图，仅作为非官方个人主题素材，不包含在 MIT 代码许可证中。

新增主题中的人物肖像与参考素材不包含在 MIT 代码许可证中。公开再分发或商业使用前，请自行确认相关授权。
